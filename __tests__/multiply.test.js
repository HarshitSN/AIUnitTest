// Test file for: multiply.js
// Generated by AI Test Generator

const { describe, test, expect  } = require('@jest/globals');

describe('multiply', () => {
  // Test cases that define their own variables and test the core logic
  describe('core functionality', () => {
    test('should perform basic multiplication', () => {
      // Arrange
      const x = 5;
      const y = 10;

      // Act
      const result = x * y;

      // Assert
      expect(result).toBe(50);
    });

    test('should handle edge case: zero multiplication', () => {
      // Arrange
      const x = 0;
      const y = 10;

      // Act
      const result = x * y;

      // Assert
      expect(result).toBe(0);
    });

    test('should handle edge case: negative multiplication', () => {
      // Arrange
      const x = -5;
      const y = -10;

      // Act
      const result = x * y;

      // Assert
      expect(result).toBe(50);
    });

    test('should handle edge case: decimal multiplication', () => {
      // Arrange
      const x = 5.5;
      const y = 10.2;

      // Act
      const result = x * y;

      // Assert
      expect(result).toBeCloseTo(56.1);
    });

    test('should handle edge case: large numbers', () => {
      // Arrange
      const x = 500000;
      const y = 200000;

      // Act
      const result = x * y;

      // Assert
      expect(result).toBe(100000000000);
    });
  });

  describe('error handling', () => {
    test('should return NaN for non-numeric inputs', () => {
      // Arrange
      const x = 'five';
      const y = 10;

      // Act
      const result = x * y;

      // Assert
      expect(result).toBeNaN();
    });
  });
});
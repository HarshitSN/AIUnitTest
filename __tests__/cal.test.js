// Test file for: cal.js
// Generated by AI Test Generator
// Tests verify EXACT code behavior, no assumptions

const { describe, test, expect } = require('@jest/globals');
// Import the Calculator class
const Calculator = require('../cal.js');

describe('Calculator', () => {
  describe('add', () => {
    test('should add two numbers', () => {
      const calculator = new Calculator();
      const result = calculator.add(5, 3);
      expect(result).toBe(8);
    });

    test('should throw error for non-numeric inputs', () => {
      const calculator = new Calculator();
      expect(() => calculator.add('a', 3)).toThrowError('Both arguments must be numbers');
    });

    test('should handle edge case: zero', () => {
      const calculator = new Calculator();
      const result = calculator.add(0, 5);
      expect(result).toBe(5);
    });

    test('should handle edge case: negative numbers', () => {
      const calculator = new Calculator();
      const result = calculator.add(-5, -3);
      expect(result).toBe(-8);
    });
  });

  describe('subtract', () => {
    test('should subtract two numbers', () => {
      const calculator = new Calculator();
      const result = calculator.subtract(5, 3);
      expect(result).toBe(2);
    });

    test('should throw error for non-numeric inputs', () => {
      const calculator = new Calculator();
      expect(() => calculator.subtract('a', 3)).toThrowError('Both arguments must be numbers');
    });

    test('should handle edge case: zero', () => {
      const calculator = new Calculator();
      const result = calculator.subtract(0, 5);
      expect(result).toBe(-5);
    });

    test('should handle edge case: negative numbers', () => {
      const calculator = new Calculator();
      const result = calculator.subtract(-5, -3);
      expect(result).toBe(-2);
    });
  });

  describe('multiply', () => {
    test('should multiply two numbers', () => {
      const calculator = new Calculator();
      const result = calculator.multiply(5, 3);
      expect(result).toBe(15);
    });

    test('should throw error for non-numeric inputs', () => {
      const calculator = new Calculator();
      expect(() => calculator.multiply('a', 3)).toThrowError('Both arguments must be numbers');
    });

    test('should handle edge case: zero', () => {
      const calculator = new Calculator();
      const result = calculator.multiply(0, 5);
      expect(result).toBe(0);
    });

    test('should handle edge case: negative numbers', () => {
      const calculator = new Calculator();
      const result = calculator.multiply(-5, -3);
      expect(result).toBe(15);
    });
  });

  describe('divide', () => {
    test('should divide two numbers', () => {
      const calculator = new Calculator();
      const result = calculator.divide(5, 3);
      expect(result).toBe(1.6666666666666667);
    });

    test('should throw error for non-numeric inputs', () => {
      const calculator = new Calculator();
      expect(() => calculator.divide('a', 3)).toThrowError('Both arguments must be numbers');
    });

    test('should throw error for division by zero', () => {
      const calculator = new Calculator();
      expect(() => calculator.divide(5, 0)).toThrowError('Division by zero');
    });

    test('should handle edge case: zero', () => {
      const calculator = new Calculator();
      const result = calculator.divide(0, 5);
      expect(result).toBe(0);
    });

    test('should handle edge case: negative numbers', () => {
      const calculator = new Calculator();
      const result = calculator.divide(-5, -3);
      expect(result).toBe(1.6666666666666667);
    });
  });

  describe('power', () => {
    test('should calculate power of two numbers', () => {
      const calculator = new Calculator();
      const result = calculator.power(5, 3);
      expect(result).toBe(125);
    });

    test('should throw error for non-numeric inputs', () => {
      const calculator = new Calculator();
      expect(() => calculator.power('a', 3)).toThrowError('Both arguments must be numbers');
    });

    test('should throw error for negative exponent', () => {
      const calculator = new Calculator();
      expect(() => calculator.power(5, -3)).toThrowError('Exponent must be non-negative');
    });

    test('should handle edge case: zero', () => {
      const calculator = new Calculator();
      const result = calculator.power(0, 5);
      expect(result).toBe(0);
    });

    test('should handle edge case: negative numbers', () => {
      const calculator = new Calculator();
      const result = calculator.power(-5, 3);
      expect(result).toBe(-125);
    });
  });

  describe('factorial', () => {
    test('should calculate factorial of a number', () => {
      const calculator = new Calculator();
      const result = calculator.factorial(5);
      expect(result).toBe(120);
    });

    test('should throw error for non-numeric inputs', () => {
      const calculator = new Calculator();
      expect(() => calculator.factorial('a')).toThrowError('Argument must be a number');
    });

    test('should throw error for negative numbers', () => {
      const calculator = new Calculator();
      expect(() => calculator.factorial(-5)).toThrowError(
        'Factorial is not defined for negative numbers'
      );
    });

    test('should handle edge case: zero', () => {
      const calculator = new Calculator();
      const result = calculator.factorial(0);
      expect(result).toBe(1);
    });

    test('should handle edge case: one', () => {
      const calculator = new Calculator();
      const result = calculator.factorial(1);
      expect(result).toBe(1);
    });
  });
});

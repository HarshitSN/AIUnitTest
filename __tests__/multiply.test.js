// Test file for: multiply.js
// Generated by AI Test Generator
// Tests verify EXACT code behavior, no assumptions

const { describe, test, expect } = require('@jest/globals');
// Import the multiply function
const multiply = require('../multiply.js');

describe('Multiply Function', () => {
  describe('Basic Functionality', () => {
    test('should multiply two numbers correctly', () => {
      const result = multiply(5, 1);
      expect(result).toBe(5);
    });

    test('should multiply two numbers with different signs correctly', () => {
      const result = multiply(5, -1);
      expect(result).toBe(-5);
    });

    test('should multiply two negative numbers correctly', () => {
      const result = multiply(-5, -1);
      expect(result).toBe(5);
    });
  });

  describe('Boundary Value Analysis', () => {
    test('should multiply zero correctly', () => {
      const result = multiply(0, 1);
      expect(result).toBe(0);
    });

    test('should multiply zero with a negative number correctly', () => {
      const result = multiply(0, -1);
      expect(result).toBe(0);
    });

    test('should multiply a large number correctly', () => {
      const result = multiply(1000000, 1);
      expect(result).toBe(1000000);
    });

    test('should multiply a large number with a negative number correctly', () => {
      const result = multiply(1000000, -1);
      expect(result).toBe(-1000000);
    });
  });

  describe('Equivalence Class Partitioning', () => {
    test('should multiply two positive numbers correctly', () => {
      const result = multiply(5, 1);
      expect(result).toBe(5);
    });

    test('should multiply two negative numbers correctly', () => {
      const result = multiply(-5, -1);
      expect(result).toBe(5);
    });

    test('should multiply a positive number with a negative number correctly', () => {
      const result = multiply(5, -1);
      expect(result).toBe(-5);
    });
  });

  describe('Exact Behavior Verification', () => {
    test('should return NaN for invalid input', () => {
      const result = multiply(NaN, 1);
      expect(result).toBeNaN();
    });

    test('should return NaN for non-numeric input', () => {
      const result = multiply('a', 1);
      expect(result).toBeNaN();
    });
  });
});

#!/usr/bin/env node

// Show Node.js version requirement
const [major] = process.version
  .match(/^v(\d+)\./)
  .slice(1)
  .map(Number);
if (major < 14) {
  console.error('Error: Node.js 14 or higher is required');
  process.exit(1);
}

// Load the main module
require('../src');
